Scenario 1: Black Friday OOMKilled

kubectl exec deployment/frontend-app -n demo -- /bin/sh -c 'a=1; while true; do a=$a$a; done'

kube_pod_container_status_last_terminated_reason{reason="OOMKilled", namespace="demo"}



Scenario 2: Dynamic Pricing CPU Throttling

kubectl exec deployment/backend-api -n demo -- /bin/sh -c 'for i in 1 2 3 4; do while true; do x=1; done & done; wait'

kubectl delete pods -l app=backend-api -n demo


rate(container_cpu_usage_seconds_total{namespace="demo"}[1m])
rate(container_cpu_usage_seconds_total{namespace="demo", container="backend"}[1m])


Scenario 3: Microservice Outage

# Break (Run this where you saved nw-block.yaml)
kubectl apply -f nw-block.yaml -n demo
# Fix
kubectl delete networkpolicy block-backend-access -n demo


Run this query to search the whole cluster for Traefik Ingress timeouts: 
{namespace=~".+"} |= " 504 " |= "/api/stats"

